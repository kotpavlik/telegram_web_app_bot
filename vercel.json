{
    "version": 2,
    "builds": [{
        "src": "index.mjs",
        "use": "@now/node"
    }],
    "routes": [{

        "src": "/(.*)",

        "dest": "index.mjs"
    }],
    "rewrites": [{ "source": "/v1/(.*)", "destination": "/v1" }]
}